'use strict';

/**
 * ligne-commande controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ligne-commande.ligne-commande');
